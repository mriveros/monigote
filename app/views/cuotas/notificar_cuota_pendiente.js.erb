<% if @enviado == true %>

  setTimeout( function(){ 
 
    swal("Notificar Cuotas","Notificación enviada exitosamente!", "success");

    }, 1000);

<% else %>
  
  setTimeout( function(){ 

    swal("Notificar Cuotas", "No fue posible enviar la notificación","error");

  }, 1000);

<% end %>