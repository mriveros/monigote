<% if @guardado_ok == true %>
 
  $("#<%= params[:window_id]%>").modal('hide');
  $("#form_buscar_pagos_cuotas_id").val('<%=@pago_salario.id%>');
  $("#form_buscar_pagos_cuotas_fecha").val('');
  $("#form_buscar_pagos_cuotas_mes_periodo").val('');
  $("#form_buscar_pagos_cuotas_anho_periodo").val('');
  $("#form_buscar_pagos_cuotas_hacienda_id").val('');
  $("#form_buscar_pagos_cuotas_total_salario").val('');
  $("#form_buscar_pagos_cuotas_total_adelantos").val('');
  $("#form_buscar_pagos_cuotas_total_descuentos").val('');
  $("#form_buscar_pagos_cuotas_total_remuneraciones_extras").val('');
  $("#form_buscar_pagos_cuotas_monto_total_pagado").val('');

  setTimeout( function(){ 
 
    swal("Cuotas","Cuotas generadas exitosamente!", "success");

    }, 1000);

 	$("#form-buscar-pagos-cuotas").submit();

<% else %>
	
	$("<div class='col-sm-12' id='msg-guardar-pago-cuota' style='color:#000;padding-top:5px;font-size:10px;background: #f8dfdf;border-radius:5px;text-align:center;'><%= @msg %></div>").insertBefore("#form-agregar-pago-cuota");
 	
  setTimeout( function(){ 

    swal("Cuotas", "Error al Generar las cuotas. <%=@msg%> Intente más tarde","error");

  }, 1000);

<% end %>